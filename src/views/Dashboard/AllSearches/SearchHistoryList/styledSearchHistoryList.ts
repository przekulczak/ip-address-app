import styled from 'styled-components';
import { variables } from '../../../../styleConfig/variables';

export const StyledHistorySearchList = styled.div`
  overflow-y: auto;
`;

export const StyledEmptyHistoryIndicatior = styled.p`
  margin-top: ${variables.mainSpacing};
`;
