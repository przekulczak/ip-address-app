import { FC } from 'react';
import { StyledSearchContainer } from './styledSearchContainer';

const SearchContainer: FC = ({ children }) => <StyledSearchContainer>{children}</StyledSearchContainer>;

export default SearchContainer;
